# Runtime mode: 'sim' reads from simulator file, 'hw' reads from device node
SENSOR_SOURCE=sim

# Sampling interval in seconds
SAMPLE_INTERVAL=1

# Host device to map as /dev/hwsensor inside the sensor container.
# On a real embedded target replace with the actual device path:
#   HW_DEVICE=/dev/ttyUSB0   (serial sensor)
#   HW_DEVICE=/dev/video0    (camera)
#   HW_DEVICE=/dev/i2c-1     (I2C bus)
HW_DEVICE=/dev/urandom

# Port exposed by the metrics dashboard on the host
METRICS_PORT=8089
